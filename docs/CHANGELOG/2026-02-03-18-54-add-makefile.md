# Add Makefile

**Date**: 2026-02-03 18:54:00

## Summary

Add Makefile with development shortcuts for installing dependencies and running services.

## Changes

- Added: `Makefile` with `install` and `dev` targets

## Affected Components

- `Makefile` - New file providing:
  - `make install` - Installs frontend (pnpm) and backend (uv) dependencies
  - `make dev` - Runs docker compose up with build
